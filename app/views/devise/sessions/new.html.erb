<%= render 'home/header' %>
<%= render 'home/footer' %>

<div id="userlogin-modal" class="absolute top-24 right-4 z-50">
    <div class="bg-white rounded-lg shadow-lg max-w-lg w-full p-6 relative">
        <button class="text-white hover:bg-red-800 absolute right-2 top-2 bg-primary px-2 py-1 rounded text-sm" onclick="document.getElementById('userlogin-modal').classList.add('hidden');">&times;</button>
        <div class="mt-8">
          <%= render 'devise/sessions/form', resource: User.new , resource_name: :user %>
        </div>
    </div>
</div>
